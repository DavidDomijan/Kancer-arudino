
int inPin = 3;
int x[7];
void setup() {
  // put your setup code here, to run once:
pinMode(inPin, INPUT);

}

void loop() {
  // put your main code here, to run repeatedly:
val = digitalRead(inPin);
if(digitalRead)== high
  {
    if(digitalRead(inPin)== low )
      {
        delay(500);
        //start
        if(digitalRead(inPin) == low)
          {
            delay(1000);
            // upisivanje bitova
            for(int i=7;i>=0;i--)
            {
              x[i]== digitalRead(inPin);
              delay(1000);
            }
           
          }
      }
  }

  // prebacivanje 8 BITNOG zapisa u ASCII
  

  
}
